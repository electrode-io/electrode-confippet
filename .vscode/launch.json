{
    "version": "0.2.0",
    "configurations": [
            {
                "name": "Launch via npx",
                "type": "node",
                "request": "launch",
                "cwd": "${workspaceFolder}",
                "runtimeExecutable": "npx",
                "runtimeArgs": ["mocha", "-c", "test/spec"]
              } 
    ]
}